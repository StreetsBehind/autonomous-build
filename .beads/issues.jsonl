{"id":"autonomous-build-1fp","title":"Borrow beads-quality patterns from refs/ skills","description":"Borrow the high-value patterns from refs/design-to-beads and refs/epic-quality-pass into the live workflow. Scope: post-compose quality-pass skill, test-plan metadata in formulas, sizing audit + plan.md coverage check in /compose, freshness check in /build-next. Skip the heavy patterns (3-pass subagent review, lossless-conversion mandate, density tracking) — they assume bespoke beads, not formula pours. See session transcript 2026-05-28 for the full rationale.","status":"closed","priority":2,"issue_type":"epic","owner":"cstaulbee@gmail.com","created_at":"2026-05-28T18:56:40Z","created_by":"cstaulbee","updated_at":"2026-05-28T19:43:49Z","closed_at":"2026-05-28T19:43:49Z","close_reason":"All children completed: yk9, 4j4, 4t9, ud0, s9f, a2m, shh, 5au, 66t, 5s4, 74i","dependencies":[{"issue_id":"autonomous-build-1fp","depends_on_id":"autonomous-build-1zq","type":"parent-child","created_at":"2026-05-28T12:56:43Z","created_by":"cstaulbee","metadata":"{}"}]}
{"id":"autonomous-build-1zq","title":"Workflow improvements","description":"Anchor epic for all improvement tasks filed by /retro runs against real apps. Each retro creates a per-app sub-epic under this.","status":"open","priority":1,"issue_type":"epic","owner":"cstaulbee@gmail.com","created_at":"2026-05-28T18:09:41Z","created_by":"cstaulbee","updated_at":"2026-05-28T18:09:41Z"}
{"id":"autonomous-build-1zq.1","title":"Add /split skill: take an oversized bead + seam, produce N atomic child beads with dep rewiring","description":"WHY: /quality-pass and /compose's sizing audit detect oversized beads but the human has to manually re-pour formulas or edit plan.md. For \"decompose into smallest atomic parts\" to be mechanical rather than aspirational, splitting needs to be a one-command operation — currently the biggest friction point between /compose and /loop /build-next.\n\nWHAT TO DO:\n- Add `skills/split/SKILL.md` with frontmatter (name=split, triggers on \"/split\", \"split this bead\", \"atomize bead X\").\n- Inputs: bead ID + optional seam description. If seam omitted, refuse with a prompt — splitting without a seam is just guessing.\n- Process:\n  1. Read source bead (`bd show $id --json`). Read its parent epic, dependents, dependencies.\n  2. From the seam + AC, propose N child beads with: distinct titles, derived ACs, file/path ownership boundaries (basis for the filesTouched work), and a fanout dep graph (sequential = child_n+1 depends on child_n; else parallel siblings).\n  3. Print the proposed split. Do NOT mutate without explicit user confirmation.\n  4. On confirm: `bd create --parent \u003cepic\u003e` each child; `bd dep add` for child↔child and child↔external (preserve incoming/outgoing edges from the source bead); close the source bead with reason \"superseded by \u003cids\u003e\" (verify with `bd update --help` whether a `superseded` status exists; if not, use closed + reason).\n  5. Re-validate `bd dep cycles`.\n- Read-only escape: if a quality-pass-style audit shows the proposed children would still be oversized (ACs\u003e6 etc.), refuse and recommend a different seam.\n\nLink from /quality-pass output: skills/quality-pass/SKILL.md should mention /split as the natural follow-on for beads scoring \u003c95 on sizing penalties.\n\nACCEPTANCE (self-verifiable):\n- `skills/split/SKILL.md` exists with frontmatter `name: split`.\n- File contains literal phrases \"seam\", \"confirm before mutate\", \"bd dep add\", \"superseded\".\n- `skills/quality-pass/SKILL.md` mentions `/split` as the follow-on for oversized beads.\n- README.md pipeline section names /split alongside /compose and /quality-pass.\n- A junction/symlink is created in ~/.claude/skills/split (or installer task autonomous-build-t9i handles it).\n","status":"closed","priority":2,"issue_type":"feature","owner":"cstaulbee@gmail.com","created_at":"2026-05-28T20:07:24Z","created_by":"cstaulbee","updated_at":"2026-05-28T20:14:02Z","closed_at":"2026-05-28T20:14:02Z","close_reason":"skills/split/SKILL.md created with frontmatter + required phrases; quality-pass remediation row references /split; README pipeline + repo-layout updated; symlink created in ~/.claude/skills/split; gate BOM-fix side-included","labels":["workflow-improvement"],"dependencies":[{"issue_id":"autonomous-build-1zq.1","depends_on_id":"autonomous-build-1zq","type":"parent-child","created_at":"2026-05-28T14:07:23Z","created_by":"cstaulbee","metadata":"{}"}]}
{"id":"autonomous-build-1zq.2","title":"Add filesTouched bead metadata + conflict-aware dispatch filter in /build-batch","description":"WHY: /build-batch currently relies on the post-merge gate to catch sibling-bead file conflicts (see skills/build-batch/SKILL.md line 330 — explicit concession). Every collision burns a full worker run + a gate run. We already write testPlanFile via metadata in compose Step 4d (autonomous-build-5au); extending the same mechanism to filesTouched is cheap and turns the parallelism from \"hopefully non-overlapping\" into actually conflict-free.\n\nWHAT TO DO:\n- Formula schema: add a `files` array under `[steps.*]` blocks in formulas/*.formula.toml (paths the step's bead may touch, including globs). Variable-substituted the same way `testPlan.file` is. Document in formulas/README.md with an example.\n- compose: in Step 4d (where testPlan metadata is written), additionally parse the step's `files` array and write to bead metadata as `filesTouched` (string array). Apply variable substitution. If a step has no `files`, skip — same pattern as testPlan.\n- build-batch Dispatch-Bead: read `$bead.metadata.filesTouched`. Reject the candidate if its glob-expanded set intersects any in-flight pipeline's filesTouched, OR any bead currently in $mergeQueue / $mergeInFlight. Log the skip (\"[SKIP] $id deferred — would conflict with $other on $sharedPaths\"). Pick the next non-conflicting candidate. If none, fall through to the normal \"no free slot\" state.\n- Beads with no filesTouched declared: fall back to current dispatch behavior, but print a one-line warning (\"[WARN] $id has no filesTouched — relying on post-merge gate; consider adding `files` to the formula step\") so formula gaps surface.\n- Update existing formulas (crud-feature, app-skeleton) to declare `files` per step.\n- Remove the line-330 acknowledgement in build-batch/SKILL.md (or update it to describe the new behavior).\n\nACCEPTANCE (self-verifiable):\n- formulas/crud-feature.formula.toml and formulas/app-skeleton.formula.toml both contain at least one `files = [...]` entry under a `[steps.*]` block.\n- formulas/README.md documents the `files` field with an example.\n- skills/compose/SKILL.md describes the filesTouched metadata write (in Step 4d or a new step).\n- skills/build-batch/SKILL.md Dispatch-Bead section includes the conflict filter — the literal phrase \"filesTouched\" must appear in the dispatch decision logic.\n- skills/build-batch/SKILL.md no longer contains the original \"post-merge gate is what catches the conflict\" sentence (or has been updated to describe the new filter).\n","status":"closed","priority":2,"issue_type":"feature","owner":"cstaulbee@gmail.com","created_at":"2026-05-28T20:07:25Z","created_by":"cstaulbee","updated_at":"2026-05-28T20:19:40Z","closed_at":"2026-05-28T20:19:40Z","close_reason":"files= declared on every step in both formulas (8+6); formulas/README.md documents the field with schema + glob rules + fallback; compose Step 4d writes filesTouched alongside testPlan; build-batch Phase 1.1 filters candidates via Test-FilesTouched-Intersect, Dispatch-Bead re-checks + carries filesTouched, helper documented; do-not section line-330 rewritten; both formulas still parse via bd cook --mode=runtime","labels":["workflow-improvement"],"dependencies":[{"issue_id":"autonomous-build-1zq.2","depends_on_id":"autonomous-build-1zq","type":"parent-child","created_at":"2026-05-28T14:07:25Z","created_by":"cstaulbee","metadata":"{}"}]}
{"id":"autonomous-build-1zq.3","title":"Expand formula library beyond crud-feature/app-skeleton (auth, page, migration, background-job, integration)","description":"WHY: compose forbids ad-hoc `bd create` (skills/compose/SKILL.md line 200) but only two formulas exist (crud-feature, app-skeleton). Any work the library doesn't cover gets shoehorned into crud-feature with awkward vars, or the rule gets broken silently. The \"smallest atomic parts\" goal needs a richer pattern library.\n\nWHAT TO DO (this bead is a decomposition deliverable — the work is filing the sub-tree, not writing the formulas):\n\n1. Create a sub-epic under autonomous-build-1zq titled \"Formula library expansion v1\".\n2. Under that sub-epic, file 5 child task beads — one per formula:\n   - `auth-email-password.formula.toml` — email/password auth: schema, endpoints, UI. Sized per layer; consider that auth touches escalation rules (touches-auth label triggers /build-next block — design the formula so layers can be implemented without crossing the auth-decision boundary).\n   - `page.formula.toml` — single UI page: route + component + test + (optional) data-fetch hook.\n   - `migration.formula.toml` — DB schema migration only, no app code. Includes up/down. Acceptance must verify schema check, not just file existence.\n   - `background-job.formula.toml` — scheduled or queue-triggered job + handler + test.\n   - `integration-http.formula.toml` — outbound HTTP integration with retries + secret handling. Secret handling triggers /build-next escalation by design — the formula should structure work so the non-secret pieces can ship while the secret-handling bead waits for human input.\n\n3. Each child bead's acceptance: `formulas/\u003cname\u003e.formula.toml` exists; `bd formula list` includes it; `bd cook \u003cname\u003e --dry-run` against representative vars from a sample plan.md exits 0. Decompose internal layers into [steps.*] blocks declaring `files` (see filesTouched bead) and `testPlan`.\n\nACCEPTANCE for THIS bead (self-verifiable):\n- A sub-epic exists under autonomous-build-1zq titled exactly \"Formula library expansion v1\" (verifiable via `bd list --parent autonomous-build-1zq --type=epic`).\n- 5 child task beads exist under that sub-epic with the formula names listed above (verifiable via `bd list --parent \u003csub-epic-id\u003e`).\n- Each child has acceptance criteria matching the pattern in item 3.\n- This bead closes once the sub-epic + children are filed. Implementation of individual formulas is the children's work, not this bead's.\n","status":"closed","priority":3,"issue_type":"feature","owner":"cstaulbee@gmail.com","created_at":"2026-05-28T20:07:27Z","created_by":"cstaulbee","updated_at":"2026-05-28T20:22:41Z","closed_at":"2026-05-28T20:22:41Z","close_reason":"Sub-epic autonomous-build-1zq.6 (\"Formula library expansion v1\") created with 5 child task beads (1zq.6.1..5), one per formula: auth-email-password, page, migration, background-job, integration-http. Each child has self-verifiable AC. No dep cycles. Implementation of individual formulas remains the children's work.","labels":["workflow-improvement"],"dependencies":[{"issue_id":"autonomous-build-1zq.3","depends_on_id":"autonomous-build-1zq","type":"parent-child","created_at":"2026-05-28T14:07:27Z","created_by":"cstaulbee","metadata":"{}"}]}
{"id":"autonomous-build-1zq.4","title":"Emit plan.lock.json from /vision; have /compose read the lock instead of regex-parsing plan.md","description":"WHY: skills/compose/SKILL.md Step 5 (lines 107-117) regex-parses plan.md for feature names — depends on em-dash placement, bolded labels, exact markdown shape. A typo silently drops a feature, caught only by the end-of-pour coverage check. A machine-readable mirror eliminates the parse fragility without removing the human-readable plan.md.\n\nWHAT TO DO:\n- Define a JSON Schema at `schemas/plan.lock.schema.json` with the structured equivalent of plan.md:\n  - `stack`: object keyed by layer (language, backend, frontend, database, orm, auth, hosting, tests, lint) → `{choice, why}`\n  - `dataModel`: array of `{entity, fields, relationships, notes}`\n  - `featureOrder`: array of `{name, formulas: [string], vars: object}`\n  - `crossFeatureDependencies`: array of `{blocked, blocker, reason}`\n  - `escalationBudget`: `{maxSessionCostUsd, maxFailuresPerTask, additionalBlockTriggers: [string]}`\n  - `openQuestions`: array of `{question, blockingCompose: bool}`\n  - `incomplete`: bool (true if openQuestions has any blockingCompose=true entries)\n\n- /vision: after writing plan.md, also write `plan.lock.json` with the same content in structured form. Validate against the schema before writing. Even when openQuestions is non-empty, write the lock with `incomplete: true` so /compose can refuse cleanly with a structured reason.\n\n- /compose: read plan.lock.json first. If `incomplete: true`, refuse with the listed open questions. If plan.lock.json missing, fall back to the current plan.md regex parse with a deprecation warning (\"plan.lock.json missing — falling back to plan.md parse; rerun /vision to generate the lock\"). When present, ignore the markdown for structured fields (features, vars, deps) — markdown remains the human narrative.\n\n- Document the schema in a new `docs/PLAN_LOCK.md` and reference it from docs/ARCHITECTURE.md §\"/vision\".\n\n- Update README.md pipeline diagram to show plan.md + plan.lock.json as paired outputs of /vision.\n\nACCEPTANCE (self-verifiable):\n- `schemas/plan.lock.schema.json` exists and is valid JSON Schema (draft 2020-12 or compatible).\n- `docs/PLAN_LOCK.md` exists.\n- `skills/vision/SKILL.md` Outputs section mentions plan.lock.json.\n- `skills/compose/SKILL.md` Step 4 or 5 reads from plan.lock.json before plan.md.\n- `README.md` pipeline section names plan.lock.json alongside plan.md.\n- Backwards compatibility: a plan.md without an adjacent plan.lock.json still works (with deprecation warning).\n","notes":"Freshness note (2026-05-28): compose Step 5 regex parse has moved to lines 117-139 (was 107-117 when bead was filed); the underlying gap (regex-based feature extraction) is still real and unchanged.","status":"closed","priority":3,"issue_type":"task","owner":"cstaulbee@gmail.com","created_at":"2026-05-28T20:07:29Z","created_by":"cstaulbee","updated_at":"2026-05-28T20:52:16Z","closed_at":"2026-05-28T20:52:16Z","close_reason":"Closed","labels":["workflow-improvement"],"dependencies":[{"issue_id":"autonomous-build-1zq.4","depends_on_id":"autonomous-build-1zq","type":"parent-child","created_at":"2026-05-28T14:07:28Z","created_by":"cstaulbee","metadata":"{}"}]}
{"id":"autonomous-build-1zq.5","title":"Polish orchestration: explicit path staging, remote-aware pull, builder-driven completion signal","description":"Bundle of three small orchestration fixes called out during workflow review. Independent of the larger improvements; file together for cohesion.\n\n1. **Explicit path staging in build-next Step 9.**\n   skills/build-next/SKILL.md line 163 uses `git add -A`, AFTER the gate's pre-commit safety scan has already run. Anything created between the gate and the commit (kickoff receipts mishandled, scratch files, an .env an agent created to test something) is unscreened by the safety check. Replace with explicit staging derived from the kickoff's ownership boundaries — `target/jankurai/kickoff-$id.json` enumerates the files the bead may touch. Stage those paths only.\n\n2. **Remote-aware pull in build-batch Merge-And-Close.**\n   skills/build-batch/SKILL.md line 245: `git pull --ff-only origin main 2\u003e\u00261 | Out-Null` — the comment says \"ignore if no remote configured\" but the code just suppresses the error. The subsequent merge still runs against the (potentially stale) local main. Replace with:\n   ```powershell\n   if ((git remote) -ne $null -and (git remote) -ne '') {\n     git pull --ff-only origin main\n     if ($LASTEXITCODE -ne 0) { \u003cabort merge, block bead\u003e }\n   }\n   ```\n\n3. **Builder-driven completion signal in build-batch poll loop.**\n   skills/build-batch/SKILL.md line 164 polls every 10s × N workers × ~30 min wall = a lot of Opus tokens spent reading idle TaskOutput. The marker is currently parsed from worker stdout. Two cheaper alternatives:\n   - Workers write `\u003cworktree\u003e/.bd-build-complete.json` when done (same JSON content as the current BUILD_COMPLETE marker). Orchestrator watches the filesystem (PowerShell `Register-ObjectEvent` on FileSystemWatcher, or the Monitor tool with the marker path).\n   - Or: have the worker `Set-Content` a sentinel and the orchestrator uses a longer sleep (60s) — less elegant but a 6× reduction without infrastructure work.\n   The first option is the \"real\" fix; the second is the quick-win fallback if the first hits tool-availability snags.\n\nACCEPTANCE (self-verifiable):\n- skills/build-next/SKILL.md Step 9 no longer contains the literal string \"git add -A\"; contains language about staging from kickoff ownership / explicit paths.\n- skills/build-batch/SKILL.md Merge-And-Close gates the pull with a `git remote` check (literal phrase \"git remote\" near the pull command).\n- skills/build-batch/SKILL.md poll loop describes a marker-file / filesystem-watch mechanism (literal phrase \".bd-build-complete\" or `FileSystemWatcher`), OR documents the 60s-sleep fallback with a TODO referencing the marker-file approach.\n","notes":"Freshness note (2026-05-28): line numbers drifted (build-next git add -A is line 162 not 163; build-batch pull is line 301 not 245; sleep is line 176 not 164) but all three constructs are unchanged and still match the bead's diagnosis.","status":"closed","priority":3,"issue_type":"task","owner":"cstaulbee@gmail.com","created_at":"2026-05-28T20:07:30Z","created_by":"cstaulbee","updated_at":"2026-05-28T20:55:34Z","closed_at":"2026-05-28T20:55:34Z","close_reason":"Closed","labels":["workflow-improvement"],"dependencies":[{"issue_id":"autonomous-build-1zq.5","depends_on_id":"autonomous-build-1zq","type":"parent-child","created_at":"2026-05-28T14:07:30Z","created_by":"cstaulbee","metadata":"{}"}]}
{"id":"autonomous-build-1zq.6","title":"Formula library expansion v1","description":"Sub-epic for the v1 formula library expansion (autonomous-build-1zq.3). Children: auth-email-password, page, migration, background-job, integration-http. Each is a single formula authored against the conventions in formulas/README.md (files=, [steps.testPlan], multi-glob alternatives for stack independence). When all five close, the formula library covers ~80% of the patterns plan.md actually requests.","status":"open","priority":2,"issue_type":"epic","owner":"cstaulbee@gmail.com","created_at":"2026-05-28T20:21:52Z","created_by":"cstaulbee","updated_at":"2026-05-28T20:21:52Z","labels":["workflow-improvement"],"dependencies":[{"issue_id":"autonomous-build-1zq.6","depends_on_id":"autonomous-build-1zq","type":"parent-child","created_at":"2026-05-28T14:21:51Z","created_by":"cstaulbee","metadata":"{}"}]}
{"id":"autonomous-build-1zq.6.1","title":"Write formulas/auth-email-password.formula.toml","description":"WHY: every non-trivial app needs auth, and crud-feature shoehorns are wrong shape (auth is cross-cutting, not entity-CRUD). plan.md picks for \"Habits with login\" currently have nowhere to land.\n\nWHAT TO DO: write `formulas/auth-email-password.formula.toml` covering email + password auth:\n\nVariables:\n- `hash_strategy` (default `argon2`; alternatives `bcrypt`, `scrypt`)\n- `session_strategy` (default `server-session`; alternative `jwt`)\n- `session_ttl_minutes` (default `1440`)\n- `require_email_verification` (default `false`)\n\nSteps (each as a `[[steps]]` block with `files = [...]` and where applicable `[steps.testPlan]`):\n1. **schema** — users + sessions tables (or sessions store keyed by session_strategy). Files: migrations/*_auth.*, models/User.*, models/Session.*.\n2. **password-hashing** — password hash + verify functions per hash_strategy. Files: services/auth/passwords.*, tests/services/auth/passwords.*. testPlan: hash deterministic check, verify roundtrip, timing-safe compare.\n3. **register-endpoint** — POST /auth/register. Files: routes/auth/register.*, tests/endpoints/auth/register.*. testPlan: 201 on valid, 400 on missing fields, 409 on duplicate email.\n4. **login-endpoint** — POST /auth/login. Files: routes/auth/login.*, tests/endpoints/auth/login.*. testPlan: 200 + session cookie/token on valid, 401 on bad password, 401 on unknown user.\n5. **session-middleware** — request gate that resolves session → user. Files: middleware/session.*, tests/middleware/session.*. testPlan: anonymous request → 401; valid session → user populated; expired → 401.\n6. **logout-endpoint** — POST /auth/logout. Files: routes/auth/logout.*, tests/endpoints/auth/logout.*. testPlan: invalidates session; subsequent request → 401.\n\nImportant: every step must include a `labels = [\"touches-auth\"]` declaration. /build-next escalates touches-auth as a blocker (per docs/ESCALATION_RULES.md) — the formula SHOULD make every auth-related step block on first encounter so the human confirms the cryptographic choices. The escalation pre-check is the design, not a bug.\n\nDo NOT include OAuth, magic-link, MFA, or password-reset in this v1 formula. Each of those is its own follow-on formula (auth-oauth, auth-magic-link, auth-mfa, auth-password-reset). Bias toward the smallest possible \"users can sign up and log in\" cluster.\n\nACCEPTANCE (self-verifiable):\n- `formulas/auth-email-password.formula.toml` exists at repo root under formulas/.\n- `bd formula list` includes `auth-email-password` (verify by grepping the output).\n- `bd cook auth-email-password --mode=runtime --var hash_strategy=argon2 --var session_strategy=server-session --var session_ttl_minutes=1440 --var require_email_verification=false` exits 0 and emits a parseable JSON proto.\n- Every `[[steps]]` block declares both `files = [...]` and `labels = [\"touches-auth\"]`.\n- Steps that ship code with tests declare a `[steps.testPlan]` sub-table.\n- `formulas/README.md` does not need a new section — the field schema is already documented; just keep this formula consistent with the conventions there.\n","status":"closed","priority":2,"issue_type":"task","owner":"cstaulbee@gmail.com","created_at":"2026-05-28T20:22:00Z","created_by":"cstaulbee","updated_at":"2026-05-28T20:41:31Z","closed_at":"2026-05-28T20:41:31Z","close_reason":"formulas/auth-email-password.formula.toml created; bd formula list includes it; bd cook --mode=runtime exits 0; every step declares files= and labels=touches-auth; 5 of 6 steps declare [steps.testPlan] (schema correctly excluded matching crud-feature precedent)","labels":["touches-auth","workflow-improvement"],"dependencies":[{"issue_id":"autonomous-build-1zq.6.1","depends_on_id":"autonomous-build-1zq.6","type":"parent-child","created_at":"2026-05-28T14:21:59Z","created_by":"cstaulbee","metadata":"{}"}]}
{"id":"autonomous-build-1zq.6.2","title":"Write formulas/page.formula.toml","description":"WHY: many features are \"add a page\" — a settings screen, an about page, a dashboard widget container. crud-feature is overkill (no entity); ad-hoc bd create is forbidden. Need a formula for the smallest UI unit.\n\nWHAT TO DO: write `formulas/page.formula.toml` for a single named page with optional data fetch:\n\nVariables:\n- `page_name` (required, e.g. \"Settings\", \"Dashboard\")\n- `route_path` (required, e.g. \"/settings\", \"/dashboard\")\n- `fetches_data` (default `false`) — if true, page gets a data hook + endpoint dependency\n- `data_endpoint_var` (optional) — the data endpoint to fetch from (only required when fetches_data=true)\n\nSteps:\n1. **route** — register the route in the framework's router. Files: routes/index.*, src/router.*, app/{{route_path}}/page.*. Small step, no testPlan.\n2. **component** — the page component, list view, or container. Files: pages/{{page_name}}.*, components/pages/{{page_name}}.*, app/{{route_path}}/page.*.\n3. **data-hook** (skipped when fetches_data=false) — fetches data from data_endpoint_var. Files: hooks/use{{page_name}}.*, src/hooks/use{{page_name}}.*. testPlan: loading state, error state, success state with data.\n4. **page-test** — component-level test. Files: tests/ui/pages/{{page_name}}.*. testPlan: renders without crashing; if fetches_data: shows loading/error/data states.\n\nImportant:\n- The skip-at-pour-time pattern (used in crud-feature's UI steps with `include_ui`) applies here too: if `fetches_data=false`, the data-hook step is skipped. Document the convention.\n- The formula must NOT bake in a specific frontend framework — use multi-glob files lists (pages/X.* OR app/X/page.* OR src/pages/X.*) so it works for Next.js, Vite-React, SvelteKit equally.\n\nACCEPTANCE (self-verifiable):\n- `formulas/page.formula.toml` exists.\n- `bd formula list` includes `page`.\n- `bd cook page --mode=runtime --var page_name=Settings --var route_path=/settings --var fetches_data=false` exits 0.\n- `bd cook page --mode=runtime --var page_name=Dashboard --var route_path=/dashboard --var fetches_data=true --var data_endpoint_var=dashboard-summary` exits 0 AND the proto includes the data-hook step (verify by grepping the JSON).\n- Every `[[steps]]` block declares `files = [...]`.\n- The component step + page-test step both declare `[steps.testPlan]`.\n","status":"in_progress","priority":2,"issue_type":"task","owner":"cstaulbee@gmail.com","created_at":"2026-05-28T20:22:01Z","created_by":"cstaulbee","updated_at":"2026-05-28T20:56:05Z","labels":["workflow-improvement"],"dependencies":[{"issue_id":"autonomous-build-1zq.6.2","depends_on_id":"autonomous-build-1zq.6","type":"parent-child","created_at":"2026-05-28T14:22:01Z","created_by":"cstaulbee","metadata":"{}"}]}
{"id":"autonomous-build-1zq.6.3","title":"Write formulas/migration.formula.toml","description":"WHY: standalone schema changes (add column, add index, backfill) don't fit crud-feature's \"full slice\" shape. Currently the only escape is reaching for bd create, which is forbidden. Many plans need one-off migrations as their own atomic unit.\n\nWHAT TO DO: write `formulas/migration.formula.toml` for a single schema migration with no app-code changes:\n\nVariables:\n- `migration_name` (required, e.g. \"add-archived-flag-to-habits\")\n- `up_outline` (required) — human-readable description of what `up` does (e.g. \"ALTER TABLE habits ADD COLUMN archived BOOLEAN NOT NULL DEFAULT false\")\n- `down_outline` (required) — what the rollback does\n- `touches_existing_data` (default `false`) — if true, this migration runs against rows that exist in deployed environments\n\nSteps:\n1. **write-migration** — author the up + down migration files. Files: migrations/*_{{migration_name}}.*, db/migrations/*_{{migration_name}}.*. No testPlan — the next two steps verify it.\n2. **apply-fresh** — apply the migration against a fresh DB. Files: tests/migrations/{{migration_name}}.apply.*, scripts/migrate.*. testPlan: migration runs to completion against fresh DB; schema-check confirms target state.\n3. **rollback-test** — apply then revert; verify no orphaned objects. Files: tests/migrations/{{migration_name}}.rollback.*. testPlan: up then down leaves the schema identical to pre-up; column/table counts match.\n\nImportant:\n- When `touches_existing_data=true`, the write-migration step MUST include a `labels = [\"needs-decision\"]` declaration so /build-next escalates per docs/ESCALATION_RULES.md. The human must confirm the backfill strategy, locking implications, and downtime budget BEFORE the migration is written.\n- Do NOT support data backfills in this v1 formula — those need their own formula (`migration-with-backfill`) because they need batched-write strategies, progress reporting, and resumability.\n- The formula targets one migration. Multi-migration cluster changes (e.g. extracting a new table from a column) belong in a multi-step formula or a hand-decomposed sub-tree via /split.\n\nACCEPTANCE (self-verifiable):\n- `formulas/migration.formula.toml` exists.\n- `bd formula list` includes `migration`.\n- `bd cook migration --mode=runtime --var migration_name=add-archived-flag-to-habits --var up_outline=\"ALTER TABLE habits ADD COLUMN archived BOOLEAN NOT NULL DEFAULT false\" --var down_outline=\"ALTER TABLE habits DROP COLUMN archived\" --var touches_existing_data=false` exits 0.\n- Every `[[steps]]` block declares `files = [...]`.\n- apply-fresh + rollback-test declare `[steps.testPlan]`.\n- When poured with `touches_existing_data=true`, the resulting write-migration step has the `needs-decision` label (verify by grepping the proto JSON).\n","status":"open","priority":2,"issue_type":"task","owner":"cstaulbee@gmail.com","created_at":"2026-05-28T20:22:03Z","created_by":"cstaulbee","updated_at":"2026-05-28T20:22:03Z","labels":["workflow-improvement"],"dependencies":[{"issue_id":"autonomous-build-1zq.6.3","depends_on_id":"autonomous-build-1zq.6","type":"parent-child","created_at":"2026-05-28T14:22:02Z","created_by":"cstaulbee","metadata":"{}"}]}
{"id":"autonomous-build-1zq.6.4","title":"Write formulas/background-job.formula.toml","description":"WHY: scheduled cleanups, queue-driven webhooks, batched recomputes — background jobs are common but have no formula. crud-feature has no concept of \"non-request-driven work.\" Without a formula, planners default to \"we'll figure it out later,\" which means /build-next never builds it.\n\nWHAT TO DO: write `formulas/background-job.formula.toml` for a single scheduled or queue-triggered job:\n\nVariables:\n- `job_name` (required, e.g. \"daily-habit-rollup\", \"process-webhook\")\n- `trigger_type` (required: `schedule` | `queue`)\n- `schedule` (only required when trigger_type=schedule; cron expression or interval, e.g. \"0 3 * * *\")\n- `queue_name` (only required when trigger_type=queue)\n- `idempotent` (default `true`) — declares whether the job tolerates re-runs; the formula uses this to decide whether to scaffold an idempotency key check\n\nSteps:\n1. **handler** — the job function itself. Files: jobs/{{job_name}}.*, src/jobs/{{job_name}}.*.\n2. **trigger-wiring** — register the job with the scheduler (cron config, schedule registration) OR the queue subscription. Files: config/jobs.*, src/scheduler.*, src/workers/{{job_name}}.*. No testPlan — wiring is verified by integration test in the next step.\n3. **idempotency-guard** (skipped when idempotent=true, i.e. when the handler is naturally idempotent and needs no key check) — adds a per-invocation key check. Files: jobs/{{job_name}}.guard.*, services/idempotency.*. testPlan: duplicate invocation returns early; first invocation runs.\n4. **handler-test** — unit test for the handler function. Files: tests/jobs/{{job_name}}.*. testPlan: handler runs to completion on representative input; handler is observably idempotent under repeated invocation if idempotent=true; on failure handler emits the expected error signal (re-raise for queue retry, log + skip for cron).\n\nImportant:\n- This formula targets one job. Multi-stage pipelines (e.g. extract → transform → load workflows) need a different formula or a hand-decomposed sub-tree.\n- Do NOT scaffold a worker process here. If the app doesn't have a worker, that's a separate `app-skeleton`-level concern.\n- The handler-test must hit the handler directly with a fake input, not go through the trigger. Trigger-side integration tests (cron firing, queue draining) are out of scope for v1.\n\nACCEPTANCE (self-verifiable):\n- `formulas/background-job.formula.toml` exists.\n- `bd formula list` includes `background-job`.\n- `bd cook background-job --mode=runtime --var job_name=daily-habit-rollup --var trigger_type=schedule --var schedule=\"0 3 * * *\" --var idempotent=true` exits 0.\n- `bd cook background-job --mode=runtime --var job_name=process-webhook --var trigger_type=queue --var queue_name=webhooks --var idempotent=false` exits 0 AND the proto INCLUDES the idempotency-guard step (verify by grepping).\n- Every `[[steps]]` block declares `files = [...]`.\n- handler + handler-test + idempotency-guard (when present) declare `[steps.testPlan]`.\n","status":"open","priority":2,"issue_type":"task","owner":"cstaulbee@gmail.com","created_at":"2026-05-28T20:22:04Z","created_by":"cstaulbee","updated_at":"2026-05-28T20:22:04Z","labels":["workflow-improvement"],"dependencies":[{"issue_id":"autonomous-build-1zq.6.4","depends_on_id":"autonomous-build-1zq.6","type":"parent-child","created_at":"2026-05-28T14:22:04Z","created_by":"cstaulbee","metadata":"{}"}]}
{"id":"autonomous-build-1zq.6.5","title":"Write formulas/integration-http.formula.toml","description":"WHY: every app eventually integrates with an outbound HTTP API (payment, email, third-party SaaS). Currently no formula covers this; the secret-handling step would touch escalation rules. A formula that structures the work so non-secret pieces can ship in parallel while secret handling waits for human input is exactly the \"smallest atomic parts + orchestrate\" goal.\n\nWHAT TO DO: write `formulas/integration-http.formula.toml` for an outbound HTTP client integration:\n\nVariables:\n- `integration_name` (required, e.g. \"stripe\", \"sendgrid\", \"internal-billing\")\n- `base_url` (required)\n- `auth_strategy` (required: `api-key` | `oauth-client-credentials` | `hmac-signed` | `none`)\n- `retry_policy` (default `\"exponential-3-attempts\"`; alternatives `\"linear-5-attempts\"`, `\"none\"`)\n- `timeout_seconds` (default `30`)\n\nSteps:\n1. **client** — the HTTP client class/module with retry policy applied. No secret handling here yet — accepts credentials as a constructor/init parameter. Files: services/integrations/{{integration_name}}/client.*, src/integrations/{{integration_name}}/client.*. testPlan: client constructs with sample creds; client honors retry_policy on 5xx; client respects timeout_seconds.\n2. **secret-handling** — wires credentials from env/secret store into client construction. Files: config/secrets/{{integration_name}}.*, services/secrets.*. **MUST declare `labels = [\"touches-secrets\"]`** so /build-next escalates per docs/ESCALATION_RULES.md. The human decides the secret store, rotation policy, and access-control before this step runs.\n3. **happy-path-test** — integration test against a mocked HTTP server or replay fixtures. Files: tests/integrations/{{integration_name}}/happy.*. testPlan: representative successful call returns parsed response; client makes the expected requests in the expected order.\n4. **error-path-test** — integration test for retry behavior + error surfaces. Files: tests/integrations/{{integration_name}}/errors.*. testPlan: 5xx triggers retry per policy; 4xx surfaces typed error; timeout surfaces typed error; final failure after retries surfaces typed error.\n\nImportant:\n- Steps 1, 3, 4 are independent of secret-handling and SHIP IN PARALLEL with secret-handling waiting. The formula's needs= declarations should NOT make step 1 depend on step 2 — only `secret-handling` blocks on human input.\n- Do NOT mock secret handling in tests. Tests construct the client with hardcoded fake credentials directly; secret handling is a separate plumbing concern verified once by /build-next's gate (or manually after the human confirms).\n- Do NOT add request signing, OAuth refresh flows, or rate-limit handling in v1. Each is its own follow-on formula (`integration-http-oauth-refresh`, `integration-http-rate-limited`).\n\nACCEPTANCE (self-verifiable):\n- `formulas/integration-http.formula.toml` exists.\n- `bd formula list` includes `integration-http`.\n- `bd cook integration-http --mode=runtime --var integration_name=stripe --var base_url=https://api.stripe.com --var auth_strategy=api-key --var retry_policy=exponential-3-attempts --var timeout_seconds=30` exits 0.\n- secret-handling step declares `labels = [\"touches-secrets\"]` (verify by grepping the proto JSON for `touches-secrets`).\n- Step `needs:` graph allows client / happy-path-test / error-path-test to run independently of secret-handling (verify by inspecting `needs` arrays — none of these three should list `secret-handling` as a need).\n- Every `[[steps]]` block declares `files = [...]`.\n- client + happy-path-test + error-path-test declare `[steps.testPlan]`.\n","status":"open","priority":2,"issue_type":"task","owner":"cstaulbee@gmail.com","created_at":"2026-05-28T20:22:05Z","created_by":"cstaulbee","updated_at":"2026-05-28T20:22:05Z","labels":["touches-secrets","workflow-improvement"],"dependencies":[{"issue_id":"autonomous-build-1zq.6.5","depends_on_id":"autonomous-build-1zq.6","type":"parent-child","created_at":"2026-05-28T14:22:05Z","created_by":"cstaulbee","metadata":"{}"}]}
{"id":"autonomous-build-37h","title":"Create beads-builder subagent in .claude/agents/","description":"The worker side of /build-batch. Wraps build-next Steps 3-9 (read issue, escalation pre-check, jankurai kickoff, implement, run post-build-gate, commit on bead/$id) but does NOT do worktree create/remove (orchestrator does) or close (orchestrator does after successful merge). Emits a structured \u003c!-- BUILD_COMPLETE:{beadId,status,commitSha} --\u003e marker on stdout. Status values: closed-ready-to-merge | blocked | failed. New file: .claude/agents/beads-builder.md.","status":"closed","priority":2,"issue_type":"task","owner":"cstaulbee@gmail.com","created_at":"2026-05-28T19:24:37Z","created_by":"cstaulbee","updated_at":"2026-05-28T19:26:45Z","closed_at":"2026-05-28T19:26:45Z","close_reason":"Created .claude/agents/beads-builder.md with input/output contracts, 8-step process, and explicit non-responsibilities (no claim/close/worktree-mgmt/merge). Subagent emits a single BUILD_COMPLETE marker the orchestrator parses.","dependencies":[{"issue_id":"autonomous-build-37h","depends_on_id":"autonomous-build-frp","type":"parent-child","created_at":"2026-05-28T13:25:11Z","created_by":"cstaulbee","metadata":"{}"}]}
{"id":"autonomous-build-4j4","title":"/build-next: support meta-work mode (no Jankurai when no AGENTS.md)","description":"/build-next assumes the repo is a Jankurai-tracked app scaffolded by /compose (has AGENTS.md, agent/baselines/, target/jankurai/ gitignored). When run on autonomous-build itself (meta-work editing skills/ and formulas/), none of that exists — Step 6 (jankurai kickoff) would refuse, Step 8's jankurai pass in post-build-gate is irrelevant. WHAT TO DO: add a meta-mode detection at the top of skills/build-next/SKILL.md — if AGENTS.md is absent at repo root, switch to meta-mode: (a) skip Step 5 worktree creation OR keep it (decide based on whether bd worktree still makes sense for skill/formula edits — probably yes for isolation), (b) skip Step 6 jankurai kickoff entirely, (c) in Step 8 run only the non-jankurai parts of post-build-gate.ps1, (d) commit + close steps unchanged. Document the mode switch in the skill. Caught while trying to /build-next the workflow-improvements epic, 2026-05-28.","status":"closed","priority":2,"issue_type":"bug","assignee":"cstaulbee","owner":"cstaulbee@gmail.com","created_at":"2026-05-28T19:01:27Z","created_by":"cstaulbee","updated_at":"2026-05-28T19:06:02Z","closed_at":"2026-05-28T19:06:02Z","close_reason":"Closed","dependencies":[{"issue_id":"autonomous-build-4j4","depends_on_id":"autonomous-build-1zq","type":"parent-child","created_at":"2026-05-28T13:02:17Z","created_by":"cstaulbee","metadata":"{}"}]}
{"id":"autonomous-build-4kx","title":"/build-batch: post-merge gate on main as defense-in-depth","description":"The per-worker quality gate runs in the worker's isolated worktree. That catches isolated regressions but not \"passed in isolation, broken when combined with sibling bead.\" Add a post-merge gate step inside the orchestrator's serialized merge: after `git merge --no-ff bead/$id` lands on main, re-run hooks/post-build-gate.ps1 on main. If it fails, `git reset --hard ORIG_HEAD` to undo the merge, mark the bead blocked with the failure summary, do NOT close the bead. This is the safety net for the (currently) no-testPlanFile world.","status":"closed","priority":2,"issue_type":"task","owner":"cstaulbee@gmail.com","created_at":"2026-05-28T19:24:52Z","created_by":"cstaulbee","updated_at":"2026-05-28T19:28:56Z","closed_at":"2026-05-28T19:28:56Z","close_reason":"Scope delivered as part of g6s: Merge-And-Close runs hooks/post-build-gate.ps1 on main after every merge and git reset --hard HEAD~1 + bd update --status=blocked on failure. Implemented in skills/build-batch/SKILL.md (Serialized merge step section).","dependencies":[{"issue_id":"autonomous-build-4kx","depends_on_id":"autonomous-build-frp","type":"parent-child","created_at":"2026-05-28T13:25:12Z","created_by":"cstaulbee","metadata":"{}"}]}
{"id":"autonomous-build-4t9","title":"Add freshness check to /build-next before claiming a bead","description":"Borrowed from refs/epic-quality-pass §Freshness check. A bead is a snapshot of the codebase at filing time. Other beads (or human work) may have landed since and silently satisfied this bead's AC. Today /build-next just claims the next ready bead and dispatches a builder — even if the work is already done. WHAT TO DO: in skills/build-next/SKILL.md, before calling bd update --status=in_progress, run a quick freshness check on the bead's load-bearing claims. If the bead says a file should not exist, glob to confirm. If it asserts wiring is missing, grep to confirm the wiring still isn't there. If it cites file sizes or line counts, spot-check. Three outcomes: (a) fresh → proceed normally; (b) drifted (details moved but gap still real) → proceed but record a 'freshness note' in the builder's prompt; (c) stale (AC already met) → close the bead with reason 'AC already met by \u003cref\u003e' and move to the next ready bead. Keep the check fast — a few grep/glob calls, not a deep code review.","status":"closed","priority":3,"issue_type":"task","assignee":"cstaulbee","owner":"cstaulbee@gmail.com","created_at":"2026-05-28T18:57:19Z","created_by":"cstaulbee","updated_at":"2026-05-28T19:15:33Z","closed_at":"2026-05-28T19:15:33Z","close_reason":"Closed","dependencies":[{"issue_id":"autonomous-build-4t9","depends_on_id":"autonomous-build-1fp","type":"parent-child","created_at":"2026-05-28T12:57:28Z","created_by":"cstaulbee","metadata":"{}"}]}
{"id":"autonomous-build-5au","title":"/compose: parse [steps.testPlan] and write bd metadata after pour","description":"Formulas now declare [steps.testPlan] blocks (autonomous-build-a2m) with file, cases, coverage fields. bd 0.55.3 silently drops these from the cooked proto, so the spawned beads have no testPlan metadata. The /quality-pass skill (autonomous-build-74i) expects metadata.testPlanFile to be populated, so compose must write it. WHAT TO DO: in skills/compose/SKILL.md, after the bd mol pour for each formula, (1) read the formula TOML directly from formulas/\u003cname\u003e.formula.toml, (2) extract each step's [steps.testPlan] block (with var substitution applied), (3) match each step.title (after var substitution) against the spawned issue.title from bd show \u003cpour-root-id\u003e --json (dependents[].title is unique within one pour), (4) for each match with a testPlan block, write bd metadata: bd update \u003cissue-id\u003e --metadata @\u003ctempfile.json\u003e where the json contains testPlanFile, testPlanCases, testPlanCoverage. Use the @file pattern because coverage strings contain semicolons that break inline JSON. This bead is the consumer for the testPlan convention added in a2m.","status":"closed","priority":2,"issue_type":"task","assignee":"cstaulbee","owner":"cstaulbee@gmail.com","created_at":"2026-05-28T19:37:15Z","created_by":"cstaulbee","updated_at":"2026-05-28T19:39:13Z","closed_at":"2026-05-28T19:39:13Z","close_reason":"Closed","dependencies":[{"issue_id":"autonomous-build-5au","depends_on_id":"autonomous-build-1zq","type":"parent-child","created_at":"2026-05-28T13:37:26Z","created_by":"cstaulbee","metadata":"{}"}]}
{"id":"autonomous-build-5s4","title":"Add plan.md coverage check to /compose: every feature must produce \u003e=1 epic","description":"Borrowed from refs/design-to-beads coverage matrix idea (stripped down). Today /compose walks plan.md §'Feature order' and pours formulas, but never verifies that every feature line in plan.md actually produced at least one epic in the DAG. A misnamed formula or a typo in plan.md silently drops features. WHAT TO DO: after pour completes in skills/compose/SKILL.md, parse plan.md §'Feature order' to extract the feature names, then diff against the titles of newly-created epics under the app's top-level epic. Report any feature name that didn't produce an epic. Same disposition as the sizing audit (sibling bead): surface in the compose summary, don't block. Skip the full word-count density matrix from the refs skill — that's only useful for freeform→beads conversion, not formula pours.","status":"closed","priority":3,"issue_type":"task","assignee":"cstaulbee","owner":"cstaulbee@gmail.com","created_at":"2026-05-28T18:57:13Z","created_by":"cstaulbee","updated_at":"2026-05-28T19:41:17Z","closed_at":"2026-05-28T19:41:17Z","close_reason":"Closed","dependencies":[{"issue_id":"autonomous-build-5s4","depends_on_id":"autonomous-build-1fp","type":"parent-child","created_at":"2026-05-28T12:57:27Z","created_by":"cstaulbee","metadata":"{}"}]}
{"id":"autonomous-build-66t","title":"Add sizing audit to /compose: flag oversized beads after pour","description":"Borrowed from refs/design-to-beads sizing rubric. Builder agents have a ~70-85K token effective budget after fixed overhead. A formula step like crud-feature.endpoints already implies 5+ files and 6 ACs in one bead — at the upper bound of what the builder can finish. WHAT TO DO: after the formula pours complete in skills/compose/SKILL.md, walk every spawned bead and apply a lightweight sizing check: count ACs, count files mentioned in description, detect cross-layer language (UI + API + DB in one bead). Flag beads exceeding thresholds (\u003e6 ACs, \u003e5 files, multi-layer scope) in the compose summary printed to the user. Do NOT auto-split or block — surface, let the user decide. This is intentionally narrower than the design-to-beads rubric (no penalty math, no scoring) because formula-poured beads are usually well-shaped; the audit just catches outliers.","status":"closed","priority":3,"issue_type":"task","assignee":"cstaulbee","owner":"cstaulbee@gmail.com","created_at":"2026-05-28T18:57:09Z","created_by":"cstaulbee","updated_at":"2026-05-28T19:40:04Z","closed_at":"2026-05-28T19:40:04Z","close_reason":"Closed","dependencies":[{"issue_id":"autonomous-build-66t","depends_on_id":"autonomous-build-1fp","type":"parent-child","created_at":"2026-05-28T12:57:27Z","created_by":"cstaulbee","metadata":"{}"}]}
{"id":"autonomous-build-74i","title":"Build /quality-pass skill: read-only audit of an epic before /loop /build-next","description":"Borrowed from refs/epic-quality-pass. Today /compose validates structurally only (no cycles, has-ready). Sized-wrong or vaguely-spec'd beads make it to /build-next and waste a worktree before failing. WHAT TO DO: create skills/quality-pass/SKILL.md as a read-only skill that takes an epic ID, scores each child bead 0-100 against a penalty rubric (sizing: files/ACs/cross-layer reach; spec concreteness: vague ACs, missing file paths, missing API contracts; context: missing links, undefined domain terms; risk: new external integrations), and proposes specific remediations to reach \u003e=95. Include the freshness check (verify the bead's load-bearing claims still hold against current code) before applying the rubric — stale beads get recommended for closure, not remediation. Output a markdown report; never call mutating bd commands. Hook it so /compose suggests running it before /loop /build-next. The skill must assert that metadata.testPlanFile is populated on each bead — depends on sibling formula-metadata work landing first.","status":"closed","priority":2,"issue_type":"feature","assignee":"cstaulbee","owner":"cstaulbee@gmail.com","created_at":"2026-05-28T18:57:03Z","created_by":"cstaulbee","updated_at":"2026-05-28T19:43:30Z","closed_at":"2026-05-28T19:43:30Z","close_reason":"Closed","dependencies":[{"issue_id":"autonomous-build-74i","depends_on_id":"autonomous-build-1fp","type":"parent-child","created_at":"2026-05-28T12:57:26Z","created_by":"cstaulbee","metadata":"{}"},{"issue_id":"autonomous-build-74i","depends_on_id":"autonomous-build-5au","type":"blocks","created_at":"2026-05-28T13:37:26Z","created_by":"cstaulbee","metadata":"{}"},{"issue_id":"autonomous-build-74i","depends_on_id":"autonomous-build-a2m","type":"blocks","created_at":"2026-05-28T12:57:29Z","created_by":"cstaulbee","metadata":"{}"}]}
{"id":"autonomous-build-a2m","title":"Add testPlan{File,Cases,Coverage} fields to formula schema and existing formulas","description":"Borrowed from refs/design-to-beads §Test Architecture. Every formula step today implies tests (endpoint-tests, ui-tests) but writes no per-bead test metadata. /build-next has no signal for where tests live, so each bead invents a new test file — at scale this fragments coverage. WHAT TO DO: (1) extend formula step schema with optional testPlanFile (string path), testPlanCases (int), testPlanCoverage (semicolon-separated string). (2) extend bd cook / bd mol pour so these flow into bd metadata at pour time. (3) retrofit formulas/crud-feature.formula.yaml and formulas/app-skeleton.formula.yaml — for each test-flavored step, set the three fields with sensible defaults parameterized by the step's variables. (4) document in formulas/README.md. This unblocks the quality-pass skill (sibling bead) which will assert metadata.testPlanFile is populated.","notes":"FOUNDATIONAL BLOCKER DISCOVERED 2026-05-28:\n\nWhile starting work on testPlan{File,Cases,Coverage} fields, I verified the existing formulas don't actually load into bd 0.55.3.\n\nWhat I tested:\n- `bd formula list` from autonomous-build repo → \"No formulas found\" despite .formula.yaml files being in the search path.\n- Copied crud-feature.formula.yaml to a fresh bd-init'd project's .beads/formulas/ → still \"No formulas found\".\n- bd formula convert only handles JSON → TOML; no YAML support.\n- Wrote a tiny JSON test formula → bd parsed it but dropped the `name:` field (canonical field is `formula = \"...\"`), dropped `variables`, dropped `acceptance` per step.\n\nComparing autonomous-build YAML vs bd's canonical schema (see beads-release.formula.toml):\n- `name:` should be `formula = \"...\"` (top-level)\n- `variables: [...]` should be `[vars.\u003cname\u003e]` tables\n- `depends_on:` per step should be `needs:`\n- `acceptance:` per step has no bd equivalent — must be embedded in description\n- `priority:` and `labels:` per step are not in the canonical example\n- bd requires top-level `type = \"workflow\"` and `version = 1`\n\nImplication: the entire /compose pipeline is non-functional against bd 0.55.3 today. `bd cook crud-feature` fails because the formula isn't recognized. Adding testPlan fields to YAML formulas bd can't load is wasted motion.\n\nRight sequence:\n1. Convert formulas to bd's canonical TOML schema (foundational — new bead)\n2. Validate the converted formulas end-to-end with bd cook + bd mol pour against a scratch project (foundational — new bead)\n3. THEN add testPlan fields to the working TOML formulas (this bead, autonomous-build-a2m)\n4. Update /compose to write the testPlan blocks as bd metadata after pour\n\nBlocking this bead until 1 and 2 are filed and closed.","status":"closed","priority":2,"issue_type":"task","assignee":"cstaulbee","owner":"cstaulbee@gmail.com","created_at":"2026-05-28T18:56:55Z","created_by":"cstaulbee","updated_at":"2026-05-28T19:37:33Z","closed_at":"2026-05-28T19:37:33Z","close_reason":"Closed","dependencies":[{"issue_id":"autonomous-build-a2m","depends_on_id":"autonomous-build-1fp","type":"parent-child","created_at":"2026-05-28T12:57:25Z","created_by":"cstaulbee","metadata":"{}"},{"issue_id":"autonomous-build-a2m","depends_on_id":"autonomous-build-s9f","type":"blocks","created_at":"2026-05-28T13:13:53Z","created_by":"cstaulbee","metadata":"{}"}]}
{"id":"autonomous-build-frp","title":"Parallel build orchestrator (/build-batch)","description":"Add an in-process orchestrator skill that fans out concurrent build-next-style work across N workers. Each worker runs in its own bead worktree; the orchestrator holds the merge lock and serializes merges to main. Settled in chat 2026-05-28: in-process orchestrator (Task fanout), serialized merges to main, post-merge gate on main as defense-in-depth. Defers: testPlanFile in formulas (separate epic a2m), stage labels for recovery, build-history.jsonl telemetry hook.","status":"closed","priority":2,"issue_type":"epic","owner":"cstaulbee@gmail.com","created_at":"2026-05-28T19:24:31Z","created_by":"cstaulbee","updated_at":"2026-05-28T19:29:35Z","closed_at":"2026-05-28T19:29:35Z","close_reason":"All children closed (37h beads-builder subagent, g6s orchestrator skill, 4kx post-merge gate). MVP delivered: .claude/agents/beads-builder.md + skills/build-batch/SKILL.md. Deferred to other epics: testPlanFile in formulas (a2m, blocked on TOML conversion), stage labels for recovery, .beads/build-history.jsonl telemetry hook."}
{"id":"autonomous-build-g6s","title":"Create /build-batch skill: orchestrator with poll loop, dispatch, serialized merge","description":"The orchestrator side. New file: skills/build-batch/SKILL.md. Responsibilities: (1) meta-mode guard (refuse if AGENTS.md missing — recommend /loop /build-next); (2) for each free slot, pick from bd ready (excluding epics + already-claimed), bd update --claim, bd worktree create, dispatch beads-builder via Task(subagent_type='beads-builder', run_in_background=true, description='Build $id'); (3) poll all active tasks via TaskOutput(block=false) every N seconds, parse BUILD_COMPLETE markers; (4) on success, push beadId to mergeQueue; (5) serialized merge step: when mergeInFlight is null, shift mergeQueue, merge bead/$id into main, run post-build-gate on main, bd close, bd worktree remove, release lock; (6) exit when all slots empty + merge queue empty + bd ready empty. Default workers=2. Flags: --workers N, --max-merges M (stop after M merges).","status":"closed","priority":2,"issue_type":"task","owner":"cstaulbee@gmail.com","created_at":"2026-05-28T19:24:45Z","created_by":"cstaulbee","updated_at":"2026-05-28T19:28:56Z","closed_at":"2026-05-28T19:28:56Z","close_reason":"Created skills/build-batch/SKILL.md: meta-mode guard, in-memory state (activePipelines/mergeQueue/mergeInFlight/mergedSet/blockedSet/failedSet), Dispatch-Bead via Agent(subagent_type=beads-builder, run_in_background=true), polled completion via TaskOutput, serialized Merge-And-Close with post-merge gate. Defaults workers=2, max-merges unbounded.","dependencies":[{"issue_id":"autonomous-build-g6s","depends_on_id":"autonomous-build-37h","type":"blocks","created_at":"2026-05-28T13:25:13Z","created_by":"cstaulbee","metadata":"{}"},{"issue_id":"autonomous-build-g6s","depends_on_id":"autonomous-build-frp","type":"parent-child","created_at":"2026-05-28T13:25:12Z","created_by":"cstaulbee","metadata":"{}"}]}
{"id":"autonomous-build-s9f","title":"Validate converted formulas end-to-end with bd cook + bd mol pour","description":"After formulas are converted to bd's TOML schema (sibling bead), prove they actually work end-to-end. Today the /compose skill assumes a working bd cook + bd mol pour pipeline but it has never been tested against the current bd build. WHAT TO DO: (1) in a scratch project (e.g. /tmp/bd-pour-test or a temp dir), bd init + copy the converted formulas in. (2) run bd cook app-skeleton --mode=runtime --var app_name=demo --var language=typescript --var framework=nextjs --var test_framework=vitest --var lint_tool=biome — verify the proto output is well-formed. (3) bd mol pour \u003cproto-id\u003e --parent \u003cepic-id\u003e — verify the spawned issues have correct titles, types, descriptions, and dependency edges (needs:). (4) repeat for crud-feature with a realistic entity. (5) bd ready should return the leaf tasks. (6) if anything fails or behaves unexpectedly, document the deviation in formulas/README.md and either fix the formula or escalate. Depends on the formula-conversion bead.","status":"closed","priority":1,"issue_type":"task","assignee":"cstaulbee","owner":"cstaulbee@gmail.com","created_at":"2026-05-28T19:13:46Z","created_by":"cstaulbee","updated_at":"2026-05-28T19:21:42Z","closed_at":"2026-05-28T19:21:42Z","close_reason":"Closed","dependencies":[{"issue_id":"autonomous-build-s9f","depends_on_id":"autonomous-build-1zq","type":"parent-child","created_at":"2026-05-28T13:13:51Z","created_by":"cstaulbee","metadata":"{}"},{"issue_id":"autonomous-build-s9f","depends_on_id":"autonomous-build-ud0","type":"blocks","created_at":"2026-05-28T13:13:52Z","created_by":"cstaulbee","metadata":"{}"}]}
{"id":"autonomous-build-shh","title":"/compose: bd mol pour has no --parent flag; reparent via bd dep add after pour","description":"/compose documents 'bd mol pour \u003cproto-id\u003e --parent \u003cepic-id\u003e' but bd 0.55.3's mol pour command has no --parent flag (verified during autonomous-build-s9f validation). Pouring spawns its own root epic; to put it under the app-level epic, reparent after pouring. WHAT TO DO: in skills/compose/SKILL.md Step 4, replace the two-step 'bd cook --persist + bd mol pour --parent' pattern with a one-step 'bd mol pour \u003cformula-name\u003e --var ...' followed by 'bd dep add \u003cpour-root-id\u003e \u003capp-epic-id\u003e --type parent-child'. Capture the pour-root-id from pour's output. Also: drop the unnecessary cook --persist intermediate — pour accepts formula names directly.","status":"closed","priority":2,"issue_type":"bug","assignee":"cstaulbee","owner":"cstaulbee@gmail.com","created_at":"2026-05-28T19:21:33Z","created_by":"cstaulbee","updated_at":"2026-05-28T19:38:41Z","closed_at":"2026-05-28T19:38:41Z","close_reason":"Closed","dependencies":[{"issue_id":"autonomous-build-shh","depends_on_id":"autonomous-build-1zq","type":"parent-child","created_at":"2026-05-28T13:21:38Z","created_by":"cstaulbee","metadata":"{}"}]}
{"id":"autonomous-build-t9i","title":"Add installer script that junctions autonomous-build/skills/* into ~/.claude/skills/","description":"Currently every new skill in autonomous-build/skills/ requires a manual 'mklink /J ~/.claude/skills/\u003cname\u003e ...' step before Claude Code can invoke it as a slash command. Easy to forget; caught when /quality-pass (autonomous-build-74i) wasn't immediately invocable. WHAT TO DO: add a small installer (PowerShell .ps1 or .cmd, since junctions are Windows-only and the workflow targets Windows per the shell config) that walks autonomous-build/skills/*, for each subdir checks whether ~/.claude/skills/\u003cname\u003e exists and points at the right target, and creates the missing junction with 'mklink /J' otherwise. Idempotent — re-running is safe. Document the invocation in README.md (probably 'Setup' section). Worth checking whether the same problem exists for autonomous-build/formulas/ → ~/.beads/formulas/ (it might already be junctioned but a future formula won't auto-link).","status":"open","priority":3,"issue_type":"task","owner":"cstaulbee@gmail.com","created_at":"2026-05-28T19:53:16Z","created_by":"cstaulbee","updated_at":"2026-05-28T19:53:16Z","dependencies":[{"issue_id":"autonomous-build-t9i","depends_on_id":"autonomous-build-1zq","type":"parent-child","created_at":"2026-05-28T13:53:22Z","created_by":"cstaulbee","metadata":"{}"}]}
{"id":"autonomous-build-ud0","title":"Convert formulas to bd canonical TOML schema","description":"The autonomous-build formulas use a YAML schema that bd 0.55.3 does not recognize. bd cook expects .formula.json or .formula.toml. Canonical schema differs from current YAML in several ways: top-level field is 'formula' not 'name'; variables go in [vars.\u003cname\u003e] tables; step deps use 'needs:' not 'depends_on:'; bd has no separate 'acceptance:' field (must be embedded in description); top-level 'type = workflow' and 'version = 1' are required. WHAT TO DO: (1) rewrite formulas/crud-feature.formula.yaml and formulas/app-skeleton.formula.yaml as .formula.toml files matching the canonical bd schema (use ~/Documents/Github/beads/.beads/formulas/beads-release.formula.toml as reference). (2) decide whether to embed the acceptance criteria inside each step's description as a clear 'Acceptance:' block, or to extend bd. (3) delete the .yaml files after conversion. (4) update formulas/README.md to describe the actual TOML schema, removing the speculative YAML schema sketch. Discovered 2026-05-28 while trying to add testPlan fields per autonomous-build-a2m.","status":"closed","priority":1,"issue_type":"task","assignee":"cstaulbee","owner":"cstaulbee@gmail.com","created_at":"2026-05-28T19:13:40Z","created_by":"cstaulbee","updated_at":"2026-05-28T19:19:39Z","closed_at":"2026-05-28T19:19:39Z","close_reason":"Closed","dependencies":[{"issue_id":"autonomous-build-ud0","depends_on_id":"autonomous-build-1zq","type":"parent-child","created_at":"2026-05-28T13:13:50Z","created_by":"cstaulbee","metadata":"{}"}]}
{"id":"autonomous-build-yk9","title":"/build-next: skip epics in bd ready output","description":"bd ready returns epics (which are containers, not work). /build-next would claim an epic, fail to implement it, and waste a tick. bd ready has no --type filter, so the skill has to filter client-side. WHAT TO DO: in skills/build-next/SKILL.md Step 1, change the bd ready call to fetch the full ready list and pick the first non-epic. PowerShell sketch: bd ready --json | ConvertFrom-Json | Where-Object { $_.issue_type -ne 'epic' } | Select-Object -First 1. Empty-result handling stays the same. Caught during the first attempt to run /build-next on autonomous-build's own meta-work, 2026-05-28.\n","status":"closed","priority":2,"issue_type":"bug","assignee":"cstaulbee","owner":"cstaulbee@gmail.com","created_at":"2026-05-28T19:01:21Z","created_by":"cstaulbee","updated_at":"2026-05-28T19:04:43Z","closed_at":"2026-05-28T19:04:43Z","close_reason":"Closed","dependencies":[{"issue_id":"autonomous-build-yk9","depends_on_id":"autonomous-build-1zq","type":"parent-child","created_at":"2026-05-28T13:02:16Z","created_by":"cstaulbee","metadata":"{}"}]}
