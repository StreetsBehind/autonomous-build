# Tenets: <app name>

> Generated by `/vision` from `vision.md` + `plan.lock.json` + the workflow-level [`docs/TENETS.md`](https://github.com/cstaulbee/autonomous-build/blob/main/docs/TENETS.md) in autonomous-build.
> Read this when about to make a judgment call during a build — before guessing, before escalating.
> This file is hand-editable. If you change it during a build, note why in the commit.

---

## How to use this document

1. Check the source-of-truth ordering below — most "judgment calls" are unread spec.
2. Check the inherited workflow tenets (T1–T10) — they cover the universal cases.
3. Check the app-specific tenets (A1+) — they cover what this app's vision/plan locked in.
4. If no tenet covers the question, escalate via `bd update <id> --status=blocked`.

---

## Source of truth ordering

When two signals disagree, the higher item wins. Always.

1. The quality gate (`hooks/post-build-gate.{sh,ps1}` from autonomous-build).
2. **This app's** `plan.lock.json`.
3. **This app's** `plan.md` (narrative only; lock wins where they disagree).
4. The bead spec (description + acceptance criteria + testPlan + filesTouched).
5. Formula output from `bd mol pour`.
6. **This app's** `tenets.md` (this file) — for judgment calls the above don't cover.
7. The workflow-level `docs/TENETS.md` in autonomous-build.
8. `docs/DEFAULT_STACK.md` in autonomous-build (stack decisions only).
9. Existing code in this repo (context, not contract).
10. Model intuition (last resort; treat as a guess).

---

## Inherited workflow tenets

These come from [`autonomous-build/docs/TENETS.md`](https://github.com/cstaulbee/autonomous-build/blob/main/docs/TENETS.md) and apply to every build. One-line summaries below; read the source doc for rationale and how-to-apply.

- **T1. Escalate over guess** — when in doubt, block the bead and let `/escalate` page the human.
- **T2. The gate is the contract** — never weaken or skip gate steps to make a bead pass.
- **T3. Atomic bead, atomic commit** — one bead = one logical change = one commit.
- **T4. Scope discipline — build the bead, not the project** — implement exactly the AC; no invented requirements.
- **T5. Reversibility bias** — escalate irreversible steps even if not on the hard-stop list.
- **T6. Formula precedence over ad-hoc** — stack and structure come from formulas / DEFAULT_STACK, not improvisation.
- **T7. Failure visibility** — loud + recoverable beats silent + clean. No swallowed exceptions.
- **T8. Idempotency by default** — every setup step is safely re-runnable.
- **T9. Meta vs app discipline** — workflow changes propagate to every app; app changes are local.
- **T10. plan.lock.json is the contract, plan.md is the narrative** — lock wins.

For the full text, hard-conflict resolutions, and "not tenets" list, see the workflow doc.

---

## App-specific tenets

> Each app-specific tenet has an ID (`A1`, `A2`, ...), a rule, a why, and a how-to-apply. App tenets do **not** override inherited workflow tenets — they complement them. If an app tenet conflicts with a workflow tenet, the workflow tenet wins; flag the conflict with `/flag` so it can be reviewed at the next retro.

### From vision.md §5 (non-goals)

> One tenet per non-goal. These prevent scope creep during build.

- **A1. <non-goal restated as "do not X">**
  - **Rule**: Never <X>.
  - **Why**: Explicitly excluded in vision.md §5. <one-line context if helpful>.
  - **How to apply**: If a bead's AC implicitly drifts toward <X>, flag with `/flag` rather than implementing.

<!-- Repeat A2, A3, ... for each non-goal. Omit the section if vision.md §5 is empty. -->

### From vision.md §6 (constraints)

> One tenet per binding constraint (budget, deadline, infra, privacy). Skip cosmetic constraints — they're not load-bearing during a build.

- **A_n. Privacy: <constraint>**
  - **Rule**: <derived rule, e.g. "do not introduce third-party analytics SDKs">.
  - **Why**: vision.md §6 — <quote>.
  - **How to apply**: Any task that would add an outbound network call to a non-pinned dependency is blocked pending human review.

<!-- Repeat for budget, infra, deadline as applicable. -->

### From plan.lock §stack (locked-in stack)

- **A_n. The stack is locked at /vision time**
  - **Rule**: Do not swap layers (database, framework, ORM, auth) during a build. Stack changes require re-running `/vision`.
  - **Why**: Mid-build stack changes invalidate every prior decision and most prior beads.
  - **How to apply**: If a bead seems to require a different stack layer, the bead is wrong — flag it. Do not improvise a swap.

### From plan.lock §agentConsults

> Each off-stack decision the 3-agent consult produced becomes a tenet. This prevents the same question from being re-litigated bead by bead during the build.

- **A_n. <consult question shortened>**
  - **Rule**: <the decision, restated as an imperative>.
  - **Why**: <the rationale from the consult>. Reversal cost: <reversalCost>.
  - **How to apply**: Treat as locked for this build. Re-opening the question requires re-running the consult, not a build-time decision. If you encounter a bead whose implementation would violate this decision, flag it.

<!-- Repeat for each agentConsults entry. Omit section if none. -->

### From plan.lock §openQuestions (deferred, non-blocking)

> Questions `/vision` deferred without blocking compose. Each became a default; the default is now a tenet so it doesn't get reopened mid-build.

- **A_n. <question, restated>**
  - **Rule**: Defaulted to <the default chosen by /vision>.
  - **Why**: <context from the openQuestions entry>. Not blocking, but worth flagging if you encounter the question during build.
  - **How to apply**: Use the default. If a bead requires the opposite, surface for human review at the next retro — do not flip the default during build.

<!-- Omit section if no non-blocking openQuestions. -->

---

## Escalation budget

Copied from `plan.lock.json` so the builder reads it from one place. If you change a value here, also update the lock or `/vision` will overwrite your change on next run.

- **Max session cost**: $<value>
- **Max failures per task**: <value>
- **Additional block triggers** (beyond `docs/ESCALATION_RULES.md`):
  - <trigger 1>
  - <trigger 2>

---

## When this file changes

- **During /vision**: regenerated wholesale from `vision.md` + `plan.lock.json`. Manual edits inside the "App-specific tenets" section are preserved by re-running `/vision` only if they have explicit `<!-- preserve -->` HTML comments — otherwise they're overwritten.
- **During a build**: edits should be rare and noted in the commit. If you need to change a tenet during a build, that's a signal that `/vision` mispredicted something — file a retro note so it's caught.
- **At retro**: any tenet that fired (or should have fired and didn't) is a candidate for promotion into the workflow-level `docs/TENETS.md` in autonomous-build.
